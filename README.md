# CRUD-Product
La carpeta SQL contiene el script SQL y el nombre de la base de datos elegido fue productcrud. 
Las variables para conexion SQL deben ser ajustadas en el archivo model/dbvars.php para que arranque el proyecto correctamente
